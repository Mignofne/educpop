# Public site URL (no trailing slash)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# PostgreSQL (Neon / Supabase). Leave empty to run without auth/DB.
DATABASE_URL=

# Better Auth (required when DATABASE_URL is set)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Phase 3 — Stripe
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Phase 3 — LLM generation (scripts/generate-livret-spec.mjs, future /api/generate)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Phase 2 — Strapi CMS
# STRAPI_URL=
# STRAPI_API_TOKEN=

# Newsletter (optional)
# RESEND_API_KEY=
